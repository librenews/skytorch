<!-- Messages Area -->
<div class="flex-1 overflow-y-auto p-4 lg:p-6">
  <div class="text-center py-12">
    <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-white rounded-full flex items-center justify-center mx-auto mb-4">
      <span class="text-3xl">🚀</span>
    </div>
    <h3 class="text-lg font-medium text-gray-900 mb-2">Welcome to SkyTorch</h3>
    <p class="text-gray-500 mb-6">Social AI tools with MCP integration</p>
    <button class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg transition-colors">
      Start New Chat
    </button>
  </div>
</div>

<!-- Message Input -->
<div class="bg-white border-t border-gray-200 p-4 lg:p-6">
  <div class="max-w-4xl mx-auto">
    <div class="flex space-x-4">
      <div class="flex-1 relative">
        <textarea 
          rows="1" 
          class="w-full resize-none border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          placeholder="Type your message..."
        ></textarea>
        <div class="absolute right-3 bottom-3">
          <button class="bg-blue-500 hover:bg-blue-600 text-white p-2 rounded-lg transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Modals -->
<%= render 'chat_action_modal' %>
<%= render 'settings_modal' %>
